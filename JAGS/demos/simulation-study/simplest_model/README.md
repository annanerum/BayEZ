# Storage 

This folder is empty. It exists with the only purpose of storing the cached results of the introductory, basic Hierarchical BAyesian EZ-DDM simulation (`JAGS/intro.Rmd`)

The results from this simulation are stored in .RData files that should be ignored by .gitignore